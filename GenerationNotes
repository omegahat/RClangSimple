getDiagnostic  - we add a "subtract 1 from the index" to the generated code since 0-based.

CXIndex_{s,g}etGlobalOptions needs some additional coercing of inputs and outputs to bitwise enums (CXGlobalOptFlags)

Default values for some functions (e.g. 0 for the flag)